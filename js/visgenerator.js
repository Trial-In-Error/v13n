var visGenerator = {
	addOptions : function(base,options){
		for(key in options){
			base[key] = options[key];
		}
		return base;
	},

	visualizeChart : function(options){

	},

};

var visualizations = {

}